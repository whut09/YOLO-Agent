components:
  distillation.logits:
    schema_version: component_contract.v1
    display_name: YOLO26 Logits Distillation
    category: distillation
    implementation_family: distillation.response
    paper_specific_mechanism_ids: [logits_distillation]
    source_papers: [method-profile:logits-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {student_teacher_logits: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_logits_distillation_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [same_dataset_split, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, resume, ddp, runtime_loss]
    known_risks: [teacher_response_misalignment, additional_training_memory]

  distillation.feature:
    schema_version: component_contract.v1
    display_name: YOLO26 Feature Distillation
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [feature_distillation]
    source_papers: [method-profile:feature-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.localization:
    schema_version: component_contract.v1
    display_name: YOLO26 Localization Distillation
    category: distillation
    implementation_family: distillation.localization
    paper_specific_mechanism_ids: [localization_distillation]
    source_papers: [method-profile:localization-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: decoded_box_responses
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {native_dfl_free_box_responses: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_localization_distillation_loss: true}
    runtime_payload_schema: {teacher_boxes: tensor, student_boxes: tensor, weight: float}
    evidence_protocol: [native_dfl_free_regression, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, native_dfl_free, resume, ddp]
    known_risks: [teacher_box_response_scale, additional_training_memory]

  distillation.relation:
    schema_version: component_contract.v1
    display_name: YOLO26 Relation Distillation
    category: distillation
    implementation_family: distillation.relation
    paper_specific_mechanism_ids: [relation_distillation]
    source_papers: [method-profile:relation-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_feature_relations
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, bounded_spatial_tokens: true}
    tensor_output_contract: {additive_relation_distillation_loss: true}
    runtime_payload_schema: {teacher_relations: tensor, student_relations: tensor, weight: float}
    evidence_protocol: [bounded_relation_tokens, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, token_bound, resume, ddp]
    known_risks: [quadratic_relation_cost, feature_hook_version_drift]

  distillation.attention:
    schema_version: component_contract.v1
    display_name: YOLO26 Channel Spatial Attention Distillation
    category: distillation
    implementation_family: distillation.attention
    paper_specific_mechanism_ids: [attention_distillation]
    source_papers: [method-profile:attention-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_feature_attention
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, channel_spatial_attention: true}
    tensor_output_contract: {additive_attention_distillation_loss: true}
    runtime_payload_schema: {teacher_attention: tensor, student_attention: tensor, weight: float}
    evidence_protocol: [validated_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [attention_scale_mismatch, feature_hook_version_drift]

  distillation.masked_feature:
    schema_version: component_contract.v1
    display_name: YOLO26 Masked Feature Distillation
    category: distillation
    implementation_family: distillation.masked_feature
    paper_specific_mechanism_ids: [masked_feature_distillation]
    source_papers: [method-profile:masked-feature-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_masked_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, teacher_attention_mask: true}
    tensor_output_contract: {additive_masked_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, mask: tensor, weight: float}
    evidence_protocol: [bounded_teacher_mask, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, mask_bound, resume, ddp]
    known_risks: [mask_sparsity, feature_hook_version_drift]

  distillation.quality_aware:
    schema_version: component_contract.v1
    display_name: YOLO26 Quality-Aware Distillation
    category: distillation
    implementation_family: distillation.quality_aware
    paper_specific_mechanism_ids: [quality_aware_distillation]
    source_papers: [method-profile:quality-aware-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: response_quality_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {teacher_quality_weighted_logits: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_quality_aware_distillation_loss: true}
    runtime_payload_schema: {teacher_quality: tensor, student_responses: tensor, weight: float}
    evidence_protocol: [confidence_localization_protocol, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, resume, ddp, runtime_loss]
    known_risks: [teacher_confidence_bias, additional_training_memory]

  distillation.teacher_ensemble:
    schema_version: component_contract.v1
    display_name: YOLO26 Teacher Ensemble Distillation Profile
    category: distillation
    implementation_family: distillation.teacher_ensemble
    paper_specific_mechanism_ids: [teacher_ensemble]
    source_papers: [method-profile:teacher-ensemble-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: ensemble_teacher_responses
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {multiple_frozen_teachers: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_ensemble_response_distillation_loss: true}
    runtime_payload_schema: {teacher_responses: tensor_list, student_responses: tensor, weight: float}
    evidence_protocol: [multiple_frozen_teachers, same_dataset_split, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [multi_teacher, frozen_eval_no_grad, amp, zero_weight, resume, ddp]
    known_risks: [multiple_teacher_memory, teacher_protocol_mismatch]

  distillation.source_free_teacher:
    schema_version: component_contract.v1
    display_name: YOLO26 Source-Free Teacher Distillation
    category: distillation
    implementation_family: distillation.source_free_teacher
    paper_specific_mechanism_ids: [source_free_teacher]
    source_papers: [method-profile:source-free-teacher]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {frozen_source_free_teacher: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_source_free_teacher_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [source_free_teacher_checkpoint, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight]
    known_risks: [teacher_checkpoint_missing]

  distillation.cross_domain_teacher:
    schema_version: component_contract.v1
    display_name: YOLO26 Cross-Domain Teacher Distillation
    category: distillation
    implementation_family: distillation.cross_domain_teacher
    paper_specific_mechanism_ids: [cross_domain_teacher]
    source_papers: [method-profile:cross-domain-teacher]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {cross_domain_frozen_teacher: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_cross_domain_teacher_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [cross_domain_teacher_checkpoint, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, feature_hooks]
    known_risks: [teacher_checkpoint_missing, domain_shift]

  distillation.contrastive:
    schema_version: component_contract.v1
    display_name: YOLO26 Contrastive Distillation
    category: distillation
    implementation_family: distillation.contrastive
    paper_specific_mechanism_ids: [contrastive_distillation]
    source_papers: [method-profile:contrastive-distillation]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, contrastive_pairs: true}
    tensor_output_contract: {additive_contrastive_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [validated_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, feature_hooks]
    known_risks: [batch_size_contrastive]

  distillation.yolo26_teacher_student:
    schema_version: component_contract.v1
    display_name: YOLO26 Teacher-Student Distillation
    category: distillation
    source_papers: []
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {teacher_checkpoint_frozen: true, teacher_checkpoint_sha256_bound: true, teacher_student_same_dataset: true, teacher_student_same_split: true, dataset_hash_bound: true, protocol_hash_bound: true, imgsz: 640}
    tensor_output_contract: {student_supervised_plus_distillation_loss: true, final_checkpoint_student_only: true}
    checkpoint_compatibility: unchanged_graph
    training_only: true
    inference_only: false
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [teacher_resolution, checkpoint_hash, dataset_split, shape, backward, amp, mock_trainer, runtime_loss, student_only_export, resume, matched_pilot]
    known_risks: [teacher_checkpoint_missing, teacher_hash_drift, teacher_student_feature_alignment, additional_training_memory]

  # --- Paper-specific mechanism contracts (frozen-83 gap closure) ---
  # Same YOLO26DistillationAdapter primitive as the family entries above, but
  # each paper's loss family keeps its own component id, changed variable, and
  # source paper so no two papers collapse into one generic adapter.

  distillation.pearson_feature:
    schema_version: component_contract.v1
    display_name: YOLO26 Pearson Feature Distillation (PKD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [pearson_feature_distillation]
    source_papers: [neurips:2022:631ad9ae3174bf4d6c0f6fdca77335a4-Abstract-Conference]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.richness_masked:
    schema_version: component_contract.v1
    display_name: YOLO26 Feature-Richness Masked Distillation (FRS)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [richness_masked_distillation]
    source_papers: [neurips:2021:29c0c0ee223856f336d7ea8052057753-Abstract]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.classifier_response:
    schema_version: component_contract.v1
    display_name: YOLO26 Classifier-to-Detector Response Distillation
    category: distillation
    implementation_family: distillation.response
    paper_specific_mechanism_ids: [classifier_response_distillation]
    source_papers: [neurips:2021:082a8bbf2c357c09f26675f9cf5bcba3-Abstract]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {student_teacher_logits: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_logits_distillation_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [same_dataset_split, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, resume, ddp, runtime_loss]
    known_risks: [teacher_response_misalignment, additional_training_memory]

  distillation.instance_conditional:
    schema_version: component_contract.v1
    display_name: YOLO26 Instance-Conditional Distillation (ICD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [instance_conditional_distillation]
    source_papers: [neurips:2021:892c91e0a653ba19df81a90f89d99bcd-Abstract]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.structural_similarity:
    schema_version: component_contract.v1
    display_name: YOLO26 Structural Similarity Distillation (StructKD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [structural_similarity_distillation]
    source_papers: [neurips:2022:18c0102cb7f1a02c14f0929089b2e576-Abstract-Conference]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.prediction_guided:
    schema_version: component_contract.v1
    display_name: YOLO26 Prediction-Guided Distillation (PGD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [prediction_guided_distillation]
    source_papers: [ecva:eccv2022:1356]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.hetero_assist:
    schema_version: component_contract.v1
    display_name: YOLO26 Heterogeneous Assistant Distillation (HEAD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [hetero_assist_distillation]
    source_papers: [ecva:eccv2022:2285]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.global_prototype:
    schema_version: component_contract.v1
    display_name: YOLO26 Global Prototype Distillation (GlobalKD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [global_prototype_distillation]
    source_papers: [ecva:eccv2022:2717]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.base_novel_commonality:
    schema_version: component_contract.v1
    display_name: YOLO26 Base-Novel Commonality Distillation (MFDC)
    category: distillation
    implementation_family: distillation.response
    paper_specific_mechanism_ids: [base_novel_commonality_distillation]
    source_papers: [ecva:eccv2022:3523]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {student_teacher_logits: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_logits_distillation_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [same_dataset_split, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, resume, ddp, runtime_loss]
    known_risks: [teacher_response_misalignment, additional_training_memory]

  distillation.bovw_consistency:
    schema_version: component_contract.v1
    display_name: YOLO26 Visual-Word Consistency Distillation (PA-BoVW)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [bovw_consistency_distillation]
    source_papers: [ecva:eccv2022:6004]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.glam_attention:
    schema_version: component_contract.v1
    display_name: YOLO26 Global-Local Attention Mask Distillation (GLAMD)
    category: distillation
    implementation_family: distillation.attention
    paper_specific_mechanism_ids: [glam_attention_distillation]
    source_papers: [ecva:eccv2022:6328]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.query_distillation:
    schema_version: component_contract.v1
    display_name: YOLO26 Query Distillation (DLIM-Det)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [query_distillation]
    source_papers: [ecva:eccv2024:11200]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.cross_scale_self:
    schema_version: component_contract.v1
    display_name: YOLO26 Cross-Scale Self-Distillation (MSCD)
    category: distillation
    implementation_family: distillation.feature
    paper_specific_mechanism_ids: [cross_scale_self_distillation]
    source_papers: [ecva:eccv2024:6619]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: intermediate_features
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {validated_feature_hooks: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_feature_distillation_loss: true}
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, feature_hooks, zero_weight, resume, ddp]
    known_risks: [feature_hook_version_drift, teacher_student_feature_alignment]

  distillation.early_learning:
    schema_version: component_contract.v1
    display_name: YOLO26 Early-Learning Distillation (ELDET)
    category: distillation
    implementation_family: distillation.response
    paper_specific_mechanism_ids: [early_learning_distillation]
    source_papers: [neurips:2025:6460e378f24da3a79f20ac2640732a00-Abstract-Conference]
    implementation_path: yolo_agent.components.adapters.distillation.yolo26_distillation
    adapter_class: YOLO26DistillationAdapter
    insertion_point: trainer_loss
    supported_detector_families: [yolo26]
    supported_model_patterns: [yolo26n.pt]
    supported_heads: [one_to_one]
    tensor_input_contract: {student_teacher_logits: true, shared_augmented_batch: true}
    tensor_output_contract: {additive_logits_distillation_loss: true}
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [same_dataset_split, frozen_teacher, matched_control]
    checkpoint_compatibility: unchanged_graph
    training_only: true
    changes_model_graph: false
    affects_latency: training_only
    affects_model_size: none
    supports_amp: true
    supports_ddp: true
    supports_onnx: true
    supports_tensorrt: true
    fixed_imgsz_compatible: true
    maturity: adapter_implemented
    tests_required: [shape, backward, amp, zero_weight, resume, ddp, runtime_loss]
    known_risks: [teacher_response_misalignment, additional_training_memory]
