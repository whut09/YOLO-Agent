schema_version: component_aliases.v1
canonical_components:
  - canonical_component_id: attention.deformable
    category: attention
    aliases: [deformable_attention, deformable-attention]
    semantic_aliases: [deformable attention module]
    detector_families: [transformer_detector, one_stage]
    target_error_types: [scale_variation, spatial_misalignment]
    target_metrics: [map50_95, ap_small]
    insertion_point: feature_attention
    yolo26_compatibility: adapter_required
    mapping_reason: Curated alias for deformable spatial attention components.
  - canonical_component_id: feature_pyramid.multi_scale
    category: feature_pyramid
    aliases: [multi_scale_features, multi-scale-features]
    semantic_aliases: [multi scale feature fusion]
    detector_families: [generic]
    target_error_types: [scale_variation, small_object_false_negative]
    target_metrics: [map50_95, ap_small, ap_medium, ap_large]
    insertion_point: neck_feature_pyramid
    yolo26_compatibility: adapter_required
    reusable_adapter_ids: [neck.multi_scale_fusion]
    mapping_reason: Multi-scale feature aggregation belongs to the feature-pyramid taxonomy.
  - canonical_component_id: neck.multi_scale_fusion
    category: neck
    aliases: [multi_scale_fusion, generic_multi_scale_fusion, cross_scale_fusion]
    semantic_aliases: [guarded multi scale neck fusion]
    detector_families: [yolo26]
    target_error_types: [scale_variation, small_object_false_negative]
    target_metrics: [map50_95, ap_small, ap_medium, ap_large]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Generic or cross-scale feature fusion maps to the local shape-preserving YOLO26 neck plugin, not to a complete source detector.
  - canonical_component_id: neck.gold_gather_distribute
    category: neck
    aliases: [gather_distribute_neck, gold_yolo_gather_distribute]
    semantic_aliases: [gold yolo gather and distribute neck]
    detector_families: [yolo, yolo26]
    target_error_types: [scale_variation, cross_scale_information_decay]
    target_metrics: [map50_95, ap_small, ap_medium, ap_large]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Gold-YOLO gather-distribute maps to an isolated local adaptation, not the complete detector.
  - canonical_component_id: neck.rtmdet_large_kernel
    category: neck
    aliases: [large_kernel_depthwise_conv, rtmdet_large_kernel_neck]
    semantic_aliases: [rtmdet 5x5 depthwise neck]
    detector_families: [one_stage, yolo26]
    target_error_types: [scale_variation, limited_receptive_field]
    target_metrics: [map50_95, ap_medium, ap_large]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: RTMDet large-kernel prior maps only to isolated 5x5 depthwise neck blocks.
  - canonical_component_id: neck.weighted_feature_pyramid
    category: neck
    aliases: [weighted_feature_pyramid, weighted_fpn]
    semantic_aliases: [learnable weighted feature pyramid]
    detector_families: [one_stage, yolo26]
    target_error_types: [scale_variation, cross_scale_information_decay]
    target_metrics: [map50_95, ap_small, ap_medium, ap_large]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Explicit learnable scale weighting maps to the isolated weighted feature-pyramid adapter.
  - canonical_component_id: neck.bidirectional_feature_fusion
    category: neck
    aliases: [bidirectional_feature_fusion, bidirectional_multi_scale_fusion]
    semantic_aliases: [bidirectional cross scale feature fusion]
    detector_families: [one_stage, yolo26]
    target_error_types: [scale_variation, cross_scale_information_decay]
    target_metrics: [map50_95, ap_small, recall]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Explicit top-down and bottom-up fusion maps to the isolated bidirectional adapter.
  - canonical_component_id: neck.lightweight
    category: neck
    aliases: [lightweight_neck, depthwise_lightweight_neck]
    semantic_aliases: [efficient depthwise neck]
    detector_families: [one_stage, yolo26]
    target_error_types: [latency_budget, model_size_budget]
    target_metrics: [map50_95, latency_ms, model_size_mb]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Explicit lightweight depthwise necks map to the isolated resource-guarded adapter.
  - canonical_component_id: block.reparameterized_convolution
    category: convolution_block
    aliases: [reparameterized_convolution, repconv, rep_conv, structural_reparameterization]
    semantic_aliases: [train multi branch deploy fused convolution]
    detector_families: [one_stage, yolo26]
    target_error_types: [representation_gap, latency_budget]
    target_metrics: [map50_95, latency_ms, model_size_mb]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Structural re-parameterization maps to audited training branches with explicit deploy fusion.
  - canonical_component_id: attention.channel
    category: attention
    aliases: [channel_attention, channel_attention_block]
    semantic_aliases: [channel wise feature attention]
    detector_families: [one_stage, yolo26]
    target_error_types: [representation_gap, class_confusion]
    target_metrics: [map50_95, per_class_ap, recall]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Explicit channel attention maps to the isolated shape-preserving graph adapter.
  - canonical_component_id: attention.spatial
    category: attention
    aliases: [spatial_attention, spatial_attention_block]
    semantic_aliases: [spatial feature attention mask]
    detector_families: [one_stage, yolo26]
    target_error_types: [small_object_false_negative, localization_error]
    target_metrics: [map50_95, ap_small, recall]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Explicit spatial attention maps to the isolated shape-preserving graph adapter.
  - canonical_component_id: neck.deformable_feature_aggregation
    category: neck
    aliases: [deformable_feature_aggregation, deformable_neck_aggregation]
    semantic_aliases: [deformable feature aggregation neck]
    detector_families: [one_stage, yolo26]
    target_error_types: [localization_error, spatial_misalignment]
    target_metrics: [map50_95, ap75, ap_small]
    insertion_point: before_detect_p3_p4_p5
    yolo26_compatibility: compatible
    mapping_reason: Deformable aggregation maps only when an explicit local operator dependency is available.
  - canonical_component_id: detection_head.dynamic
    category: detection_head
    aliases: [dynamic_head, dynamic-head]
    semantic_aliases: [dynamic detection head]
    detector_families: [one_stage, two_stage]
    target_error_types: [scale_variation, feature_selection]
    target_metrics: [map50_95]
    insertion_point: detection_head
    yolo26_compatibility: adapter_required
    mapping_reason: Dynamic head changes feature selection or prediction-head behavior.
  - canonical_component_id: head.p2_small_object
    category: detection_head
    aliases: [p2_head, p2_small_object_head]
    semantic_aliases: [high resolution p2 detection head]
    detector_families: [yolo26]
    target_error_types: [small_object_false_negative, stride_resolution_limit]
    target_metrics: [ap_small, map50_95]
    insertion_point: feature_pyramid_p2
    yolo26_compatibility: adapter_required
    mapping_reason: P2 small-object heads map to the audited YOLO26 graph adapter when available.
  - canonical_component_id: detection_head.learnable_proposals
    category: detection_head
    aliases: [learnable_proposals, learnable-proposals]
    semantic_aliases: [learned object queries]
    detector_families: [transformer_detector]
    target_error_types: [proposal_recall, duplicate_prediction]
    target_metrics: [map50_95, recall]
    insertion_point: query_or_proposal_initialization
    yolo26_compatibility: incompatible
    mapping_reason: Learnable proposals are detector-family head/query concepts, not YOLO26 adapters.
  - canonical_component_id: matching.hybrid
    category: matching
    aliases: [hybrid_matching, hybrid-matching]
    semantic_aliases: [hybrid assignment matching]
    detector_families: [transformer_detector, nms_free]
    target_error_types: [assignment_instability, duplicate_prediction]
    target_metrics: [map50_95, recall]
    insertion_point: target_matching
    yolo26_compatibility: adapter_required
    reusable_adapter_ids: [assigner.dual_path]
    mapping_reason: Hybrid matching changes how predictions are paired with targets.
  - canonical_component_id: assigner.task_aligned
    category: assigner
    aliases: [task_aligned_assignment, task-aligned-assignment, task_aligned_assigner]
    semantic_aliases: [task aligned assigner]
    detector_families: [one_stage, yolo26]
    target_error_types: [positive_sample_quality, classification_localization_misalignment]
    target_metrics: [map50_95, recall]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Task-aligned assignment maps to the guarded YOLO26 TOOD-TAL shadow adapter; active use still requires shadow evidence.
  - canonical_component_id: assigner.optimal_transport
    category: assigner
    aliases: [optimal_transport_assigner, ota_assignment]
    semantic_aliases: [optimal transport assignment]
    detector_families: [one_stage, yolo26]
    target_error_types: [assignment_conflict, duplicate_prediction, crowded_scene]
    target_metrics: [assignment_conflict_rate, recall, map50_95]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: OTA maps to an independent point-based Sinkhorn shadow implementation with an evidence-gated active path.
  - canonical_component_id: assigner.dynamic_smooth_label
    category: assigner
    aliases: [dynamic_smooth_labels, dynamic_smooth_label_assigner, dsla]
    semantic_aliases: [dynamic smooth label assignment]
    detector_families: [one_stage, yolo26]
    target_error_types: [assignment_boundary_instability, small_object_false_negative, confidence_localization_mismatch]
    target_metrics: [assignment_conflict_rate, ap_small, map50_95]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: DSLA maps to an independent smooth-target shadow implementation adapted to YOLO26 point candidates.
  - canonical_component_id: assigner.task_aligned_weighting
    category: assigner
    aliases: [task_aligned_weighting, task-aligned-weighting]
    semantic_aliases: [task aligned positive weighting]
    detector_families: [one_stage, yolo26]
    target_error_types: [positive_sample_quality, classification_localization_misalignment]
    target_metrics: [assignment_conflict_rate, assignment_matching_stability, recall]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Reusable task-aligned weighting maps to a point-based shadow adapter without implying the TOOD head or exact TOOD reproduction.
  - canonical_component_id: assigner.dynamic_topk
    category: assigner
    aliases: [dynamic_topk_matching, dynamic_top_k_matching]
    semantic_aliases: [dynamic positive cardinality matching]
    detector_families: [one_stage, yolo26]
    target_error_types: [false_negative, assignment_positive_ratio_low]
    target_metrics: [assignment_positive_ratio, assignment_matching_stability, recall]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Dynamic top-k matching maps to a point-based cardinality adapter and requires shadow evidence before active use.
  - canonical_component_id: assigner.quality_aware
    category: assigner
    aliases: [quality_aware_matching, quality-aware-matching]
    semantic_aliases: [classification iou quality matching]
    detector_families: [one_stage, yolo26]
    target_error_types: [confidence_localization_mismatch, high_confidence_false_positive]
    target_metrics: [assignment_conflict_rate, assignment_matching_stability, confidence_iou_correlation]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Quality-aware matching ranks point candidates by bounded classification and IoU quality while retaining native YOLO26 losses.
  - canonical_component_id: assigner.soft_label
    category: assigner
    aliases: [soft_label_assignment, soft-label-assignment]
    semantic_aliases: [soft positive assignment targets]
    detector_families: [one_stage, yolo26]
    target_error_types: [confidence_calibration_error, assignment_boundary_instability]
    target_metrics: [assignment_matching_stability, expected_calibration_error]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Soft-label assignment maps to bounded positive quality targets after point matching, not to a regression-loss replacement.
  - canonical_component_id: assigner.dual_path
    category: assigner
    aliases: [dual_path_assignment, dual-path-assignment]
    semantic_aliases: [one to many one to one assignment adaptation]
    detector_families: [nms_free, yolo26]
    target_error_types: [duplicate_prediction, assignment_path_disagreement]
    target_metrics: [assignment_positive_ratio, assignment_conflict_rate, assignment_matching_stability]
    insertion_point: both_assignment_paths
    yolo26_compatibility: compatible
    mapping_reason: Dual-path adaptation declares and shadows both native YOLO26 training paths while preserving the NMS-free inference path.
  - canonical_component_id: assigner.conflict_aware
    category: assigner
    aliases: [conflict_aware_positive_selection, conflict-aware-positive-selection]
    semantic_aliases: [ambiguous positive conflict rejection]
    detector_families: [one_stage, yolo26]
    target_error_types: [assignment_conflict, duplicate_prediction]
    target_metrics: [assignment_gt_conflict_rate, assignment_matching_stability, recall]
    insertion_point: one_to_many_assignment
    yolo26_compatibility: compatible
    mapping_reason: Conflict-aware selection rejects ambiguous point-to-GT claims using a quality margin and remains shadow-gated.
  - canonical_component_id: loss.quality.iou_aware_classification
    category: quality_estimation
    aliases: [IoU_aware_classification, iou_aware_classification]
    semantic_aliases: [iou aware classification score, localization aware classification score]
    detector_families: [yolo26]
    target_error_types: [score_localization_misalignment, ranking_error]
    target_metrics: [map50_95, precision]
    insertion_point: classification_quality_branch
    yolo26_compatibility: compatible
    mapping_reason: IoU-aware classification is an additive native-match quality target; it does not replace YOLO26 regression or assignment.
  - canonical_component_id: quality_alignment.general
    category: quality_estimation
    aliases: [classification_localization, quality_estimation, mutual_supervision]
    semantic_aliases: [classification localization alignment]
    detector_families: [generic, one_stage]
    target_error_types: [confidence_localization_mismatch, ranking_error]
    target_metrics: [map50_95, ap75, precision]
    insertion_point: classification_and_localization_training_objectives
    yolo26_compatibility: adapter_required
    reusable_adapter_ids: [loss.quality.correlation]
    mapping_reason: Generic classification-localization alignment is a method family and does not imply a specific local auxiliary-loss implementation.
  - canonical_component_id: detection_head.task_aligned
    category: detection_head
    aliases: [task_aligned_head]
    semantic_aliases: [task aligned detection head]
    detector_families: [one_stage]
    target_error_types: [classification_localization_misalignment]
    target_metrics: [map50_95, ap75]
    insertion_point: detection_head
    yolo26_compatibility: adapter_required
    mapping_reason: A task-aligned head is distinct from task-aligned assignment and needs an independent YOLO26 graph adapter.
  - canonical_component_id: loss.quality.correlation
    category: classification_loss
    aliases: [correlation_loss, quality_alignment]
    semantic_aliases: [classification localization correlation loss]
    detector_families: [yolo26]
    target_error_types: [localization_heavy, confidence_localization_mismatch]
    target_metrics: [map50_95, ap75]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Correlation Loss maps to the additive YOLO26 quality-alignment loss adapter.
  - canonical_component_id: loss.calibration.bpc
    category: calibration
    aliases: [confidence_calibration, train_time_loss, bpc_calibration]
    semantic_aliases: [bridging precision and confidence]
    detector_families: [yolo26]
    target_error_types: [high_confidence_false_positive, confidence_localization_mismatch]
    target_metrics: [precision, detection_calibration_error]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: BPC maps to a training-only confidence calibration auxiliary loss.
  - canonical_component_id: loss.quality.pseudo_iou
    category: quality_estimation
    aliases: [pseudo_iou, pseudo-iou]
    semantic_aliases: [pseudo iou quality target]
    detector_families: [yolo26]
    target_error_types: [localization_heavy, positive_sample_quality]
    target_metrics: [map50_95, ap75]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Pseudo-IoU is conservatively adapted as an auxiliary quality target without replacing the native assigner.
  - canonical_component_id: loss.quality.localization_aware
    category: quality_estimation
    aliases: [localization_aware_classification, localization-aware-classification]
    semantic_aliases: [localization aware classification, localization quality aware classification]
    detector_families: [yolo26]
    target_error_types: [localization_heavy, confidence_localization_mismatch]
    target_metrics: [map50_95, ap75, recall]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Localization-aware classification adds a matched-IoU and center-quality target as an auxiliary term.
  - canonical_component_id: loss.boundary_aware
    category: bbox_regression_loss
    aliases: [boundary_aware_loss, boundary-aware-loss]
    semantic_aliases: [boundary aware auxiliary loss, box boundary loss]
    detector_families: [yolo26]
    target_error_types: [localization_error, localization_heavy]
    target_metrics: [map50_95, ap75]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Boundary-aware regression is isolated as an additive box-edge term while native DFL-free regression remains authoritative.
  - canonical_component_id: loss.localization.uncertainty_weighted
    category: bbox_regression_loss
    aliases: [uncertainty_weighted_regression, uncertainty-weighted-regression]
    semantic_aliases: [uncertainty weighted localization loss, uncertainty aware regression]
    detector_families: [yolo26]
    target_error_types: [localization_error, localization_variance]
    target_metrics: [map50_95, ap75, recall]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Confidence-derived detached uncertainty weights an auxiliary native box residual; this is component adaptation, not exact paper reproduction.
  - canonical_component_id: loss.hard_negative_classification
    category: classification_loss
    aliases: [hard_negative_classification, hard-negative-classification]
    semantic_aliases: [hard negative classification loss, background hard negative loss]
    detector_families: [yolo26]
    target_error_types: [high_confidence_false_positive, background_confusion]
    target_metrics: [precision, false_positive_rate]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Unmatched native locations receive an additive background classification term without changing assignment.
  - canonical_component_id: loss.class_balanced_focal
    category: classification_loss
    aliases: [class_balanced_focal, class-balanced-focal]
    semantic_aliases: [class balanced focal loss, long tail focal loss]
    detector_families: [yolo26]
    target_error_types: [class_imbalance, long_tail]
    target_metrics: [per_class_recall, recall, map50_95]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Batch class-frequency weights and focal modulation are isolated as an auxiliary classification term.
  - canonical_component_id: augmentation.denoising
    category: augmentation
    aliases: [denoising, denoising_training]
    semantic_aliases: [query denoising training]
    detector_families: [transformer_detector]
    target_error_types: [matching_instability, slow_convergence]
    target_metrics: [map50_95]
    insertion_point: training_targets
    yolo26_compatibility: incompatible
    mapping_reason: Query denoising is primarily a transformer-detector training recipe.
  - canonical_component_id: feature_pyramid.standard
    category: feature_pyramid
    aliases: [feature_pyramid, feature-pyramid, fpn]
    semantic_aliases: [pyramid feature fusion]
    detector_families: [generic]
    target_error_types: [scale_variation]
    target_metrics: [map50_95, ap_small]
    insertion_point: neck_feature_pyramid
    yolo26_compatibility: adapter_required
    reusable_adapter_ids: [neck.multi_scale_fusion]
    mapping_reason: Generic feature-pyramid concepts require an explicit model-graph adapter.
  - canonical_component_id: sampling.small_object
    category: sampling
    aliases: [small_object_sampling, small-object-sampling]
    semantic_aliases:
      - small object oversampling
      - gradient based sampling
      - class imbalanced sampling
      - rare class sampling
    detector_families: [yolo26]
    target_error_types: [small_object_false_negative, class_imbalance]
    target_metrics: [ap_small, recall]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: adapter_required
    mapping_reason: Explicit image or class sampling mechanisms map to the local bounded sampler as component adaptation, not exact paper reproduction.
  - canonical_component_id: sampling.small_object_weighted
    category: sampling
    aliases: [small_object_weighted_sampling]
    semantic_aliases: [small object weighted sampling]
    detector_families: [yolo26]
    target_error_types: [small_object_false_negative]
    target_metrics: [ap_small, recall]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: compatible
    mapping_reason: Explicit small-object image exposure maps to the bounded weighted sampler; paper-specific protocols remain MethodProfiles.
  - canonical_component_id: sampling.class_balanced
    category: sampling
    aliases: [class_balanced_sampling]
    semantic_aliases: [class balanced sampling]
    detector_families: [yolo26]
    target_error_types: [class_imbalance, long_tail]
    target_metrics: [per_class_recall, map50_95]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: compatible
    mapping_reason: Class-frequency exposure is implemented independently from small-object weighting.
  - canonical_component_id: sampling.repeat_factor
    category: sampling
    aliases: [repeat_factor_sampling]
    semantic_aliases: [repeat factor sampling]
    detector_families: [yolo26]
    target_error_types: [class_imbalance, long_tail]
    target_metrics: [per_class_recall, map50_95]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: compatible
    mapping_reason: Image-level repeat factors use class image frequency and retain a separate runtime identity.
  - canonical_component_id: sampling.hard_negative_replay
    category: sampling
    aliases: [hard_negative_replay]
    semantic_aliases: [hard negative replay]
    detector_families: [yolo26]
    target_error_types: [high_confidence_false_positive, background_confusion]
    target_metrics: [precision, false_positive_rate]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: compatible
    mapping_reason: Replay requires local hard-negative evidence and is not inferred from ordinary sampling.
  - canonical_component_id: sampling.false_negative_class_boost
    category: sampling
    aliases: [false_negative_class_boost]
    semantic_aliases: [false negative class boost]
    detector_families: [yolo26]
    target_error_types: [false_negative_heavy_class]
    target_metrics: [per_class_recall, recall]
    insertion_point: train_dataloader_sampler
    yolo26_compatibility: compatible
    mapping_reason: Class exposure is driven only by local false-negative scores and target class IDs.
  - canonical_component_id: augmentation.copy_paste_rare_classes
    category: augmentation
    aliases: [copy_paste_rare_classes]
    semantic_aliases: [rare class copy paste]
    detector_families: [yolo26]
    target_error_types: [class_imbalance, long_tail]
    target_metrics: [per_class_recall, map50_95]
    insertion_point: training_data
    yolo26_compatibility: compatible
    mapping_reason: Rare-class copy-paste is an isolated train dataset transform, not generic augmentation reproduction.
  - canonical_component_id: augmentation.scale_aware_crop
    category: augmentation
    aliases: [scale_aware_crop]
    semantic_aliases: [scale aware crop]
    detector_families: [yolo26]
    target_error_types: [scale_variation, small_object_false_negative]
    target_metrics: [ap_small, map50_95]
    insertion_point: training_data
    yolo26_compatibility: compatible
    mapping_reason: Scale-aware crop synchronizes image and normalized box geometry at fixed imgsz 640.
  - canonical_component_id: augmentation.object_centric_crop
    category: augmentation
    aliases: [object_centric_crop]
    semantic_aliases: [object centric crop]
    detector_families: [yolo26]
    target_error_types: [small_object_false_negative, class_imbalance]
    target_metrics: [ap_small, recall]
    insertion_point: training_data
    yolo26_compatibility: compatible
    mapping_reason: Object-centric crop selects a labeled object center and updates boxes with the crop.
  - canonical_component_id: augmentation.multi_image_sampling_schedule
    category: augmentation
    aliases: [multi_image_sampling_schedule]
    semantic_aliases: [multi image sampling schedule]
    detector_families: [yolo26]
    target_error_types: [data_diversity, slow_convergence]
    target_metrics: [map50_95, recall]
    insertion_point: training_data
    yolo26_compatibility: compatible
    mapping_reason: Multi-image exposure is controlled by an epoch schedule and remains independent from scalar sampler weights.
  - canonical_component_id: inference.sahi_slicing
    category: slicing
    aliases: [slicing, sahi, slicing_inference, sliced_inference, high_resolution_tiling, overlap_merge]
    semantic_aliases: [tiled inference, slicing aided hyper inference]
    detector_families: [generic]
    target_error_types: [small_object_false_negative]
    target_metrics: [sliced_map50_95, sliced_ap_small, sliced_latency_ms]
    insertion_point: inference_policy
    yolo26_compatibility: compatible
    mapping_reason: SAHI slicing, high-resolution tiling, and overlap merge map to the isolated inference adapter and remain separate from standard 640 metrics.
  - canonical_component_id: inference.tiled_multi_scale
    category: slicing
    aliases: [tiled_multi_scale, multi_scale_tiling, tiled_scale_pyramid]
    semantic_aliases: [tiled multi scale inference, multi resolution tile inference]
    detector_families: [yolo26]
    target_error_types: [small_object_false_negative, scale_variation]
    target_metrics: [tiled_multi_scale_map50_95, tiled_multi_scale_ap_small, tiled_multi_scale_latency_ms]
    insertion_point: inference_policy
    yolo26_compatibility: compatible
    mapping_reason: Multi-resolution tiled views reuse one detector checkpoint and publish an isolated inference namespace.
  - canonical_component_id: inference.test_time_augmentation
    category: tta
    aliases: [test_time_augmentation, tta, multi_scale_tta, flip_tta]
    semantic_aliases: [test time augmentation, augmented inference]
    detector_families: [yolo26]
    target_error_types: [robustness, scale_variation]
    target_metrics: [tta_map50_95, tta_ap_small, tta_latency_ms]
    insertion_point: inference_policy
    yolo26_compatibility: compatible
    mapping_reason: Scale and flip inference views map to the isolated TTA adapter and never to a training augmentation recipe.
  - canonical_component_id: inference.confidence_calibration
    category: calibration
    aliases: [inference_confidence_calibration, temperature_scaling, posthoc_calibration]
    semantic_aliases: [post hoc confidence calibration, calibrated prediction scores]
    detector_families: [yolo26]
    target_error_types: [confidence_miscalibration, high_confidence_false_positive]
    target_metrics: [calibrated_map50_95, calibrated_recall]
    insertion_point: prediction_scores
    yolo26_compatibility: compatible
    mapping_reason: Post-hoc score calibration changes only inference confidence and remains distinct from train-time calibration loss.
  - canonical_component_id: inference.class_aware_thresholding
    category: threshold
    aliases: [class_aware_thresholding, per_class_threshold, class_specific_threshold]
    semantic_aliases: [per class confidence thresholds, class aware score filtering]
    detector_families: [yolo26]
    target_error_types: [class_imbalance, class_specific_false_negative]
    target_metrics: [class_threshold_map50_95, class_threshold_recall]
    insertion_point: prediction_scores
    yolo26_compatibility: compatible
    mapping_reason: Per-class validation thresholds filter inference outputs without changing detector training.
  - canonical_component_id: inference.merge_policy
    category: ensemble
    aliases: [cross_view_merge, weighted_box_fusion, nmm_merge, merge_policy_variant]
    semantic_aliases: [cross view prediction fusion, weighted box merging]
    detector_families: [yolo26]
    target_error_types: [duplicate_prediction, cross_view_overlap]
    target_metrics: [merged_map50_95, merged_recall, merged_latency_ms]
    insertion_point: cross_view_predictions
    yolo26_compatibility: compatible
    mapping_reason: NMM and weighted fusion are explicit cross-view policies; NMS requires one-to-one opt-in and is not added to the native path.
  - canonical_component_id: distillation.yolo26_teacher_student
    category: distillation
    aliases: [distillation, teacher_student_distillation]
    semantic_aliases: [knowledge distillation, teacher student knowledge distillation]
    detector_families: [yolo26]
    target_error_types: [capacity_gap, representation_quality]
    target_metrics: [map50_95, ap_small]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Distillation maps to the audited YOLO26 teacher-student runtime loss adapter.
  - canonical_component_id: distillation.logits
    category: distillation
    aliases: [logits_distillation, response_distillation, output_distillation]
    semantic_aliases: [teacher output distribution distillation]
    detector_families: [yolo26]
    target_error_types: [capacity_gap, class_confusion]
    target_metrics: [map50_95, recall]
    insertion_point: trainer_loss
    yolo26_compatibility: compatible
    mapping_reason: Response-level teacher supervision maps to the isolated logits distillation runtime.
  - canonical_component_id: distillation.feature
    category: distillation
    aliases: [feature_distillation, intermediate_feature_distillation]
    semantic_aliases: [intermediate feature imitation]
    detector_families: [yolo26]
    target_error_types: [representation_gap, small_object_false_negative]
    target_metrics: [map50_95, ap_small]
    insertion_point: intermediate_features
    yolo26_compatibility: compatible
    mapping_reason: Explicit intermediate feature alignment maps to the validated feature-hook runtime.
  - canonical_component_id: distillation.localization
    category: distillation
    aliases: [localization_distillation, box_distillation]
    semantic_aliases: [bounding box response distillation]
    detector_families: [yolo26]
    target_error_types: [localization_error, confidence_localization_mismatch]
    target_metrics: [map50_95, ap75]
    insertion_point: decoded_box_responses
    yolo26_compatibility: compatible
    mapping_reason: Box-response supervision maps to additive localization distillation without restoring DFL.
  - canonical_component_id: distillation.relation
    category: distillation
    aliases: [relation_distillation, relational_distillation]
    semantic_aliases: [feature relation knowledge distillation]
    detector_families: [yolo26]
    target_error_types: [feature_relation_gap, class_confusion]
    target_metrics: [map50_95, recall]
    insertion_point: intermediate_feature_relations
    yolo26_compatibility: compatible
    mapping_reason: Explicit feature-relation transfer maps to the bounded relation runtime.
  - canonical_component_id: distillation.attention
    category: distillation
    aliases: [attention_distillation, channel_spatial_attention_distillation]
    semantic_aliases: [channel and spatial attention distillation]
    detector_families: [yolo26]
    target_error_types: [representation_gap, small_object_false_negative]
    target_metrics: [map50_95, ap_small]
    insertion_point: intermediate_feature_attention
    yolo26_compatibility: compatible
    mapping_reason: Channel/spatial teacher attention maps to the isolated attention distillation runtime.
  - canonical_component_id: distillation.masked_feature
    category: distillation
    aliases: [masked_feature_distillation, masked_generative_distillation]
    semantic_aliases: [teacher attention masked feature distillation]
    detector_families: [yolo26]
    target_error_types: [hard_region_false_negative, representation_gap]
    target_metrics: [map50_95, recall]
    insertion_point: intermediate_masked_features
    yolo26_compatibility: compatible
    mapping_reason: Masked feature transfer maps to teacher-attention masking as component adaptation.
  - canonical_component_id: distillation.quality_aware
    category: distillation
    aliases: [quality_aware_distillation, quality_distillation]
    semantic_aliases: [teacher confidence weighted distillation]
    detector_families: [yolo26]
    target_error_types: [confidence_localization_mismatch, high_confidence_false_positive]
    target_metrics: [map50_95, precision]
    insertion_point: response_quality_loss
    yolo26_compatibility: compatible
    mapping_reason: Teacher-confidence weighting maps to the quality-aware response runtime.
  - canonical_component_id: distillation.teacher_ensemble
    category: distillation
    aliases: [teacher_ensemble_distillation, multi_teacher_distillation]
    semantic_aliases: [multiple teacher ensemble distillation]
    detector_families: [yolo26]
    target_error_types: [teacher_variance, capacity_gap]
    target_metrics: [map50_95, recall]
    insertion_point: ensemble_teacher_responses
    yolo26_compatibility: compatible
    mapping_reason: Multiple explicit local teachers map to probability-space ensemble distillation.
  - canonical_component_id: distillation.vision_language
    category: distillation
    aliases: [vision_language_distillation, visual_linguistic_distillation, visual_linguistic_knowledge]
    semantic_aliases: [visual linguistic knowledge distillation]
    detector_families: [open_vocabulary_detector, vision_language_detector]
    target_error_types: [unseen_class_recall, vocabulary_shift]
    target_metrics: [map50_95, recall]
    insertion_point: vision_language_teacher_and_classifier
    yolo26_compatibility: incompatible
    mapping_reason: Vision-language distillation changes teacher modalities or classifier semantics and is not the local YOLO26 teacher-student adapter.
  - canonical_component_id: distillation.cross_modal
    category: distillation
    aliases: [cross_modality_distillation, multimodal_knowledge_distillation]
    semantic_aliases: [cross modality knowledge distillation, distilling multimodal knowledge]
    detector_families: [multimodal_detector]
    target_error_types: [modality_gap]
    target_metrics: [map50_95, recall]
    insertion_point: multimodal_teacher_and_features
    yolo26_compatibility: incompatible
    mapping_reason: Cross-modal distillation requires modality-specific inputs and remains outside the single-modality YOLO26 adapter.
  - canonical_component_id: domain_adaptation.general
    category: domain_adaptation
    aliases: [domain_adaptation, domain-adaptation]
    semantic_aliases: [unsupervised domain adaptive detection]
    detector_families: [generic]
    target_error_types: [domain_shift]
    target_metrics: [map50_95, recall]
    insertion_point: training_data_and_features
    yolo26_compatibility: adapter_required
    mapping_reason: Domain adaptation is a broad recipe family requiring a concrete adaptation method.
  - canonical_component_id: domain_adaptation.adversarial_alignment
    category: domain_adaptation
    aliases: [adversarial_domain_alignment, domain_adversarial_training]
    semantic_aliases: [adversarial feature alignment across domains]
    yolo26_compatibility: adapter_required
    mapping_reason: Adversarial alignment requires a paper-specific discriminator and loss contract.
  - canonical_component_id: domain_adaptation.feature_alignment
    category: domain_adaptation
    aliases: [domain_feature_alignment, prototype_alignment]
    semantic_aliases: [source target feature distribution alignment]
    reusable_adapter_ids: [domain_adaptation.feature_alignment]
    yolo26_compatibility: adapter_required
    mapping_reason: Feature alignment requires explicit source and target feature payloads.
  - canonical_component_id: domain_adaptation.pseudo_label_adaptation
    category: domain_adaptation
    aliases: [pseudo_label_adaptation, confidence_mixing, self_training_adaptation]
    semantic_aliases: [target domain pseudo label self training]
    yolo26_compatibility: adapter_required
    mapping_reason: Pseudo-label adaptation requires split-safe target labels and confidence policy.
  - canonical_component_id: domain_adaptation.domain_distillation
    category: domain_adaptation
    aliases: [domain_distillation, retention_driven_knowledge_compression]
    semantic_aliases: [teacher student transfer across domains]
    yolo26_compatibility: adapter_required
    mapping_reason: Domain distillation requires a domain-bound teacher and student protocol.
  - canonical_component_id: domain_adaptation.source_free_adaptation
    category: domain_adaptation
    aliases: [source_free_adaptation, source_free_domain_adaptation]
    semantic_aliases: [source free target self training]
    yolo26_compatibility: adapter_required
    mapping_reason: Source-free adaptation cannot reuse source-target alignment payloads.
  - canonical_component_id: domain_adaptation.cross_domain_teacher
    category: domain_adaptation
    aliases: [cross_domain_teacher, expert_teacher_student]
    semantic_aliases: [cross domain teacher student adaptation]
    yolo26_compatibility: adapter_required
    mapping_reason: Cross-domain teacher methods require explicit teacher update and target split contracts.
  - canonical_component_id: domain_adaptation.contrastive_domain_alignment
    category: domain_adaptation
    aliases: [contrastive_domain_alignment, category_contrastive_alignment]
    semantic_aliases: [cross domain contrastive feature alignment]
    yolo26_compatibility: adapter_required
    mapping_reason: Contrastive alignment requires paper-specific positive and negative pair construction.
  - canonical_component_id: domain_adaptation.active_domain_adaptation
    category: domain_adaptation
    aliases: [active_domain_adaptation, active_target_sampling]
    semantic_aliases: [active learning for domain adaptation]
    yolo26_compatibility: adapter_required
    mapping_reason: Active adaptation requires an annotation acquisition protocol and cannot be inferred from generic domain shift.
  - canonical_component_id: detection_head.open_vocabulary
    category: detection_head
    aliases: [open_vocabulary_detection, open-vocabulary-detection]
    semantic_aliases: [open vocabulary object detection]
    detector_families: [open_vocabulary_detector]
    target_error_types: [unseen_class_recall, vocabulary_shift]
    target_metrics: [map50_95, recall]
    insertion_point: classifier_and_text_encoder
    yolo26_compatibility: incompatible
    mapping_reason: Open-vocabulary detection changes the classifier and detector family.
compound_aliases:
  - aliases: [small_object_multiscale_recipe]
    semantic_aliases: [small object multi scale pipeline]
    canonical_component_ids: [feature_pyramid.multi_scale, sampling.small_object]
    split_reason: The paper term combines feature aggregation and data sampling, which require separate ablations.
paper_mechanisms:
  - paper_specific_mechanism_id: logits_distillation
    aliases: [response_distillation, output_distillation]
    canonical_component_id: distillation.logits
    implementation_family: distillation.response
    changed_variables: [loss.distillation.logits.weight]
    runtime_payload_schema: {teacher_logits: tensor, student_logits: tensor, weight: float}
    evidence_protocol: [same_dataset_split, frozen_teacher, matched_control]
    required_adapter: distillation.logits
    required_evidence: [response_distillation_evidence, changed_variable, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: feature_distillation
    aliases: [intermediate_feature_distillation, decoupled_feature_distillation]
    canonical_component_id: distillation.feature
    implementation_family: distillation.feature
    changed_variables: [loss.distillation.feature.weight]
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, weight: float}
    evidence_protocol: [same_feature_hooks, frozen_teacher, matched_control]
    required_adapter: distillation.feature
    required_evidence: [feature_distillation_evidence, feature_hook_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: relation_distillation
    aliases: [relational_distillation, structural_knowledge_distillation]
    canonical_component_id: distillation.relation
    implementation_family: distillation.relation
    changed_variables: [loss.distillation.relation.weight]
    runtime_payload_schema: {teacher_relations: tensor, student_relations: tensor, weight: float}
    evidence_protocol: [bounded_relation_tokens, frozen_teacher, matched_control]
    required_adapter: distillation.relation
    required_evidence: [relation_distillation_evidence, relation_builder_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: localization_distillation
    aliases: [box_distillation, regression_distillation]
    canonical_component_id: distillation.localization
    implementation_family: distillation.localization
    changed_variables: [loss.distillation.localization.weight]
    runtime_payload_schema: {teacher_boxes: tensor, student_boxes: tensor, weight: float}
    evidence_protocol: [native_dfl_free_regression, frozen_teacher, matched_control]
    required_adapter: distillation.localization
    required_evidence: [localization_distillation_evidence, box_parameterization_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: attention_distillation
    aliases: [channel_spatial_attention_distillation, attention_transfer]
    canonical_component_id: distillation.attention
    implementation_family: distillation.attention
    changed_variables: [loss.distillation.attention.weight]
    runtime_payload_schema: {teacher_attention: tensor, student_attention: tensor, weight: float}
    evidence_protocol: [validated_feature_hooks, frozen_teacher, matched_control]
    required_adapter: distillation.attention
    required_evidence: [attention_distillation_evidence, attention_reduction_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: masked_feature_distillation
    aliases: [masked_generative_distillation, teacher_masked_feature_distillation]
    canonical_component_id: distillation.masked_feature
    implementation_family: distillation.masked_feature
    changed_variables: [loss.distillation.masked_feature.weight]
    runtime_payload_schema: {teacher_features: tensor_list, student_features: tensor_list, mask: tensor, weight: float}
    evidence_protocol: [bounded_teacher_mask, frozen_teacher, matched_control]
    required_adapter: distillation.masked_feature
    required_evidence: [masked_feature_evidence, mask_generation_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: quality_aware_distillation
    aliases: [quality_distillation, confidence_weighted_distillation]
    canonical_component_id: distillation.quality_aware
    implementation_family: distillation.quality_aware
    changed_variables: [loss.distillation.quality_aware.weight]
    runtime_payload_schema: {teacher_quality: tensor, student_responses: tensor, weight: float}
    evidence_protocol: [confidence_localization_protocol, frozen_teacher, matched_control]
    required_adapter: distillation.quality_aware
    required_evidence: [quality_aware_evidence, quality_target_contract, teacher_checkpoint]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: teacher_ensemble
    aliases: [teacher_ensemble_distillation, multi_teacher_distillation]
    canonical_component_id: distillation.teacher_ensemble
    implementation_family: distillation.teacher_ensemble
    changed_variables: [loss.distillation.teacher_ensemble.weight]
    runtime_payload_schema: {teacher_responses: tensor_list, student_responses: tensor, weight: float}
    evidence_protocol: [multiple_frozen_teachers, same_dataset_split, matched_control]
    required_adapter: distillation.teacher_ensemble
    required_evidence: [teacher_ensemble_evidence, teacher_checkpoint_set, aggregation_contract]
    compatibility: compatible
    generic_parent_component_id: distillation.yolo26_teacher_student
  - paper_specific_mechanism_id: adversarial_alignment
    aliases: [adversarial_domain_alignment, domain_adversarial_training]
    canonical_component_id: domain_adaptation.adversarial_alignment
    implementation_family: domain_adaptation.adversarial_alignment
    changed_variables: [loss.domain_adversarial_alignment.weight]
    runtime_payload_schema: {source_features: tensor_list, target_features: tensor_list, discriminator: module}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, domain_pair, source_target_split, matched_control]
    required_adapter: domain_adaptation.adversarial_alignment
    required_evidence: [adversarial_alignment_evidence, domain_labels, discriminator_contract]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: feature_alignment
    aliases: [domain_feature_alignment, prototype_alignment]
    canonical_component_id: domain_adaptation.feature_alignment
    implementation_family: domain_adaptation.feature_alignment
    changed_variables: [loss.domain_alignment.weight]
    runtime_payload_schema: {source_features: tensor_list, target_features: tensor_list, domain_ids: tensor}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, domain_pair, source_target_split, matched_control]
    required_adapter: domain_adaptation.feature_alignment
    required_evidence: [feature_alignment_evidence, domain_labels, feature_hook_contract]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: pseudo_label_adaptation
    aliases: [confidence_mixing, pseudo_labels, self_training_adaptation]
    canonical_component_id: domain_adaptation.pseudo_label_adaptation
    implementation_family: domain_adaptation.pseudo_label_adaptation
    changed_variables: [loss.domain_pseudo_label_adaptation.weight]
    runtime_payload_schema: {target_images: tensor, pseudo_labels: boxes, confidence_policy: mapping}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, target_pseudo_label_manifest, domain_pair, matched_control]
    required_adapter: domain_adaptation.pseudo_label_adaptation
    required_evidence: [pseudo_label_evidence, target_manifest_hash, confidence_policy]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: domain_distillation
    aliases: [retention_driven_knowledge_compression, cross_domain_distillation]
    canonical_component_id: domain_adaptation.domain_distillation
    implementation_family: domain_adaptation.domain_distillation
    changed_variables: [loss.domain_distillation.weight]
    runtime_payload_schema: {domain_teacher: module, target_images: tensor, student_responses: tensor}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, frozen_teacher_checkpoint, domain_pair, matched_control]
    required_adapter: domain_adaptation.domain_distillation
    required_evidence: [domain_distillation_evidence, teacher_checkpoint, target_manifest_hash]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: source_free_adaptation
    aliases: [source_free_domain_adaptation, source_free_self_training]
    canonical_component_id: domain_adaptation.source_free_adaptation
    implementation_family: domain_adaptation.source_free_adaptation
    changed_variables: [loss.domain_source_free_adaptation.weight]
    runtime_payload_schema: {source_checkpoint: path, target_images: tensor, adaptation_state: mapping}
    evidence_protocol: [target_manifest_sha256, source_model_checkpoint, source_model_protocol_hash, matched_control]
    required_adapter: domain_adaptation.source_free_adaptation
    required_evidence: [source_free_evidence, source_checkpoint, target_manifest_hash]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: cross_domain_teacher
    aliases: [expert_teacher_student, cross_domain_teacher_student]
    canonical_component_id: domain_adaptation.cross_domain_teacher
    implementation_family: domain_adaptation.cross_domain_teacher
    changed_variables: [teacher.domain_update_policy]
    runtime_payload_schema: {teacher: module, student: module, source_batch: mapping, target_batch: mapping}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, frozen_cross_domain_teacher, domain_pair, matched_control]
    required_adapter: domain_adaptation.cross_domain_teacher
    required_evidence: [cross_domain_teacher_evidence, teacher_checkpoint, update_policy]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: contrastive_domain_alignment
    aliases: [category_contrastive_alignment, contrastive_feature_alignment]
    canonical_component_id: domain_adaptation.contrastive_domain_alignment
    implementation_family: domain_adaptation.contrastive_domain_alignment
    changed_variables: [loss.domain_contrastive_alignment.weight]
    runtime_payload_schema: {source_embeddings: tensor, target_embeddings: tensor, pair_labels: tensor}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, contrastive_pair_manifest, domain_pair, matched_control]
    required_adapter: domain_adaptation.contrastive_domain_alignment
    required_evidence: [contrastive_alignment_evidence, pair_mining_contract, domain_labels]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
  - paper_specific_mechanism_id: active_domain_adaptation
    aliases: [active_target_sampling, active_domain_learning]
    canonical_component_id: domain_adaptation.active_domain_adaptation
    implementation_family: domain_adaptation.active_domain_adaptation
    changed_variables: [loss.domain_active_adaptation.weight]
    runtime_payload_schema: {target_pool: manifest, acquisition_scores: tensor, annotation_budget: integer}
    evidence_protocol: [source_manifest_sha256, target_manifest_sha256, active_query_manifest, domain_pair, matched_control]
    required_adapter: domain_adaptation.active_domain_adaptation
    required_evidence: [active_adaptation_evidence, target_pool_manifest, acquisition_policy]
    compatibility: adapter_required
    generic_parent_component_id: domain_adaptation.general
