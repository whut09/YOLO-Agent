schema_version: research_priority.v1
weights:
  coco_relevance: 15.0
  real_time_relevance: 10.0
  yolo26_compatibility: 20.0
  official_code: 8.0
  license: 3.0
  ablation_evidence: 8.0
  latency_evidence: 5.0
  size_evidence: 3.0
  single_variable: 8.0
  implementation_cost: 5.0
  training_cost: 5.0
  inference_cost: 5.0
  reproduction_risk: 8.0
category_keywords:
  backbone: [backbone, encoder, feature extractor]
  neck: [neck, feature fusion, feature pyramid fusion]
  detection_head: [detection head, prediction head, decoupled head]
  feature_pyramid: [feature pyramid, fpn, panet, pafpn, p2 head]
  attention: [attention, self-attention, cross-attention, transformer block]
  convolution_block: [convolution block, repconv, depthwise convolution, deformable convolution]
  optimizer: [optimizer, adamw, sgd, musgd]
  lr_schedule: [learning rate schedule, cosine schedule, warmup, scheduler]
  loss_schedule: [progressive loss, loss schedule]
  assigner: [assigner, task aligned, positive assignment]
  matching: [matching, one-to-one, one to one, bipartite matching]
  positive_sample_selection: [positive sample, positive selection, top-k assignment]
  bbox_regression_loss: [bounding box regression, bbox regression loss, bbox loss, iou loss, nwd, mpdiou, wiou]
  classification_loss: [classification loss, focal loss, varifocal]
  quality_estimation: [quality estimation, centerness, iou-aware, quality score]
  distillation: [distillation, knowledge distillation, teacher student]
  augmentation: [augmentation, mosaic, mixup, copy-paste, random crop]
  sampling: [sampling, oversampling, hard negative, long-tail]
  label_quality: [label noise, annotation quality, relabel, missing label]
  active_learning: [active learning, uncertainty sampling, pseudo label]
  threshold: [confidence threshold, per-class threshold, calibration]
  slicing: [sahi, slicing, tiled inference, tiling]
  tta: [tta, test time augmentation, multi-scale inference, flip inference]
  calibration: [temperature scaling, calibration, confidence calibration]
  ensemble: [ensemble, weighted box fusion, wbf]
  nms: [nms, non-maximum suppression, soft-nms, diou-nms, nms-free]
  pretraining: [pretraining, self-supervised, foundation model]
  domain_adaptation: [domain adaptation, domain transfer, cross-domain]
error_keywords:
  small_object_miss: [small object, tiny object, small-object, ap_small]
  false_negative: [false negative, missed detection, recall]
  localization_error: [localization, loose box, shifted box, bounding box regression]
  classification_error: [class confusion, classification error, long-tail]
  background_false_positive: [background false positive, hard negative, background confusion]
metric_keywords:
  map50_95: [map50-95, map50_95, coco map]
  ap_small: [ap_small, small object ap]
  recall: [recall, ar, average recall]
  latency: [latency, fps, throughput, inference speed]
  model_size: [model size, parameters, params, flops]
detector_family_keywords:
  yolo: [yolo, yolov]
  detr: [detr, transformer detector, set prediction]
  grounding_dino: [grounding dino]
  open_vocabulary: [open vocabulary, open-vocabulary, open world]
incompatibility_keywords:
  - dfl-only
  - requires anchors
  - requires roi proposal network
separate_family_keywords:
  - detr decoder
  - grounding dino
  - region proposal network
  - open vocabulary detector
direct_adapter_keywords:
  - assigner
  - matching
  - loss
  - augmentation
  - sampling
  - distillation
  - optimizer
mechanism_families:
  - family_id: small_object
    priority_rank: 1
    canonical_component_ids:
      - sampling.small_object
      - head.p2_small_object
      - inference.sahi_slicing
  - family_id: quality_alignment
    priority_rank: 2
    canonical_component_ids:
      - loss.quality.correlation
      - loss.calibration.bpc
      - loss.quality.pseudo_iou
      - quality_estimation.iou_aware_classification
      - quality_alignment.general
      - detection_head.task_aligned
  - family_id: distillation
    priority_rank: 3
    canonical_component_ids:
      - distillation.yolo26_teacher_student
  - family_id: multi_scale_neck
    priority_rank: 4
    canonical_component_ids:
      - neck.multi_scale_fusion
      - neck.gold_gather_distribute
      - neck.rtmdet_large_kernel
      - feature_pyramid.multi_scale
      - feature_pyramid.standard
non_mechanism_terms:
  task_scope:
    - object_detection
    - small_object
    - tiny_object
    - few_shot_detection
    - pedestrian_detection
    - oriented_detection
    - video_detection
    - semi_supervised
    - weak_supervision
    - incremental_detection
    - continual_detection
    - aerial_detection
    - infrared
    - event_camera
    - multimodal_detection
    - long_tailed_detection
    - real_time
  detector_family:
    - detr
    - transformer
    - yolo
    - open_vocabulary
    - nms_free_detection
    - two_stage
  separate_detector_families:
    - detr
    - oriented_detr
    - open_world_detector
    - open_vocabulary_detector
    - two_stage_open_vocabulary
    - retrieval_detector
    - grounding_dino
    - grounded_detector
    - owl_vit
    - sparse_rcnn
